# AI Code Reviewer - Client

This is the React + Vite frontend.

Run:

```bash
cd client
npm install
npm run dev
```

The dev server proxies `/api` requests to `http://localhost:4000` (see `vite.config.js`).
